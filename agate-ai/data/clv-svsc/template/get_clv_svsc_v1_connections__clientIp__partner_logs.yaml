testCases:

  - id: "{XL[testcaseName]}"
    description: "{XL[testcaseDescription]}"
    stage: "*"
    priority: HIGH

    variables:
      apiEndpoint: "{XL[apiEndpoint]}"

    steps:
      - type: REST
        op: EXEC
        command: rest.get_clv_svsc_v1_connections__clientIp__partner_logs
        endpoint: "{B[apiEndpoint]}"
        parameters:
          clientIp: "{B[clientIp]}"
          timeout: "{B[timeout]}"
        response: response_1

      - type: REST
        op: ASSERT
        response: response_1
        source: STATUS
        action: EQUALS
        expected: "{B[statusCode]}"

      - type: REST
        op: ASSERT
        condition: "{B[statusCode]} == '200' AND {XL[assert.earliest]} != NULL"
        response: response_1
        source: BODY
        action: EXISTS
        path: "{XL[assert.earliest]}"

      - type: REST
        op: ASSERT
        condition: "{B[statusCode]} == '200' AND {XL[assert.latest]} != NULL"
        response: response_1
        source: BODY
        action: EXISTS
        path: "{XL[assert.latest]}"

      - type: REST
        op: ASSERT
        condition: "{B[statusCode]} == '200' AND {XL[assert.entries]} != NULL"
        response: response_1
        source: BODY
        action: EXISTS
        path: "{XL[assert.entries]}"

openapi: 3.0.3

info:
  title: AGATE Phase 0 Broken Ref Test
  version: 1.0.0

paths:

  /users:

    get:
      operationId: getUsers

      responses:

        '200':
          description: Users returned
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/DoesNotExist'
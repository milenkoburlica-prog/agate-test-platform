openapi: 3.0.3

info:
  title: AGATE Common B
  version: 1.0.0

paths: {}

components:

  schemas:

    Address:
      type: object
      required:
        - city
        - country
      properties:

        city:
          type: string

        country:
          type: string
          enum:
            - AT
            - DE
            - CH
            - BA
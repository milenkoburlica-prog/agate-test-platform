testCases:

  - id: "{XL[testcaseName]}"
    description: "{XL[testcaseDescription]}"
    stage: "*"
    priority: HIGH

    variables:

      apiEndpoint: "{XL[apiEndpoint]}"

    steps:

      - type: REST
        op: EXEC
        command: rest.GET_clv_svsc_v1_updates_ordinations
        endpoint: "{B[apiEndpoint]}"
        updated-after: "{XL[updated-after]}"
        sv-system: "{XL[sv-system]}"
        sv-txid: "{XL[sv-txid]}"
        response: response_1

      - type: REST
        op: ASSERT
        response: response_1
        source: STATUS
        action: EQUALS
        expected: "{XL[statusCode]}"
